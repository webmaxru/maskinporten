/**
 * maskinporten — TypeScript-first client for Norway's Maskinporten.
 *
 * SCAFFOLD PLACEHOLDER. Full implementation is tracked in plan.md:
 *   - client-core-jwt        (JWT-bearer grant assertion + key loading)
 *   - client-token-flow      (getToken + TTL cache + typed errors)
 *   - client-altinn-exchange (exchangeToAltinnToken)
 *   - client-systembruker    (authorization_details RAR)
 *
 * Public API target:
 *   const client = createMaskinportenClient({ env, clientId, scope, key });
 *   await client.getToken();
 *   await client.exchangeToAltinnToken();
 */

export const version = '0.0.0';
