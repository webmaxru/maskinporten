# Documentation

Start here:

- [`prerequisites.md`](./prerequisites.md) — who needs to register what, and which secrets must never be committed.
- [`quickstart.md`](./quickstart.md) — install, use the mock, then move to real Maskinporten.
- [`../examples/basic.ts`](../examples/basic.ts) — minimal `getToken()` snippet.

Package READMEs:

- [`maskinporten`](../packages/maskinporten/README.md) — TypeScript client.
- [`maskinporten-mock`](../packages/maskinporten-mock/README.md) — local token endpoint mock.
- [`maskinporten-scopes`](../packages/scopes-catalogue/README.md) — scope catalogue and `maskinporten-wizard` CLI.
